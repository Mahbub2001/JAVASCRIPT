const div = document.getElementById("half");
//find the name of attibutes
// console.log(div.getAttribute("id"));
//set attribute
console.log(div.setAttribute("class","last"));//first is the type of attribute that you  want and then the name of the attribute that  you want.